<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit place</title>
</head>
<body>
    <form action="/places/<%=spot._id%>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="places[title]" value="<%=spot.title%>">
        </div>
        <div>
            <label for="title">Location</label>
            <input type="text" id="location" name="places[location]" value="<%=spot.location%>">
        </div>
        <button>Update Place</button>
    </form>
        <a href="/places/<%=spot._id%>"></a>
</body>
</html>